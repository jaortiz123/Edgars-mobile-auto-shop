// Deprecated legacy test referencing removed AppointmentContext.
// Replaced with a skipped placeholder to retain filename without executing obsolete logic.
import { describe, it } from 'vitest';

describe.skip('appointments.optimisticMove (deprecated)', () => {
  it('skipped - AppointmentContext was refactored', () => {
    // This test suite was removed due to AppointmentContext refactoring
    // All optimistic move tests have been moved to integration tests
    // Keeping file for reference but all tests are skipped
  });
});
